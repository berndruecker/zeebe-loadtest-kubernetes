apiVersion: apps/v1
kind: Deployment
metadata:
  name: load-starter
  labels:
    app: load-starter
spec:
  selector:
    matchLabels:
      app: load-starter
  replicas: 1
  template:
    metadata:
      labels:
        app: load-starter
    spec:
      containers:
      - name: load-starter
        image: berndruecker/zeebe-loadtest-starter:latest
        imagePullPolicy: Always
        env:
          - name: zeebe.brokerContactPoint            
            value: "zeebe-gateway:26500"
          - name: elasticsearch.url          
            value: "http://elastic-gateway:9200"
          - name: elasticsearch.currentConfigDescription
            value: "starter: 1, nodes: 3, replication, partitions: 3"


